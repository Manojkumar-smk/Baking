# Flask Configuration
FLASK_ENV=development
SECRET_KEY=dev-secret-key-change-in-production-docker
DEBUG=True

# Database Configuration
# This will be overridden by docker-compose.yml environment
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/cookie_shop_dev

# JWT Configuration
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production-docker
JWT_ACCESS_TOKEN_EXPIRES=3600
JWT_REFRESH_TOKEN_EXPIRES=2592000

# Razorpay Configuration (Test Mode)
# Replace with your actual Razorpay test keys from https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_your_key_id
RAZORPAY_KEY_SECRET=your_key_secret
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://frontend

# File Upload
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# Cloudinary Configuration (for product images)
# Get your credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=drgbtdjip
CLOUDINARY_API_KEY=221116352726669
CLOUDINARY_API_SECRET=bffdDaq2a3Op-TpKILWMDYQcaW4

# Redis Configuration (for caching)
REDIS_URL=redis://redis:6379/0

# Optional: Email Configuration
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USE_TLS=True
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
